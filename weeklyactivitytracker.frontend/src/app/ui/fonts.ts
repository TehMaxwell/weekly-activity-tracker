import { Noto_Sans, Noto_Sans_Mono } from 'next/font/google';

export const notoSans = Noto_Sans({ subsets: ['latin'], weight: ["500", "700"] });
export const notoSansMono = Noto_Sans_Mono({ subsets: ['latin'], weight: ["100", "300"]});
